<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detail Makanan</title>
    <link rel="stylesheet" href="/css/output.css">
</head>
<body class="w-full min-h-screen flex justify-center items-center">
    <div class="w-full max-w-xs lg:max-w-4xl xl:max-w-6xl mx-auto flex flex-col gap-y-6">
        <p class="text-center font-bold text-4xl">Detail Makanan</p>
        <div class="flex flex-col items-stretch lg:flex-row gap-y-4 lg:gap-y-0 lg:gap-x-4 mx-auto">
            <button class="text-bg-amber-500 text-black py-2 rounded-xl ring-2 ring-amber-500 hover:bg-amber-500 hover:text-white hover:scale-105 hover:duration-300 hover:cursor-pointer px-12">Ubah Data</button>
            <button class="text-bg-amber-500 text-black py-2 rounded-xl ring-2 ring-amber-500 hover:bg-amber-500 hover:text-white hover:scale-105 hover:duration-300 hover:cursor-pointer px-12">Hapus Data</button>
        </div>
        <div class="flex flex-col gap-y-3 ring-2 ring-amber-500 py-2 rounded-xl items-center mx-auto">
            <p class="text-center mx-auto">{{food.name}}</p>
            <p class="text-center mx-auto">{{food.price}}</p>
            <p class="text-center mx-auto">{{food.description}}</p>
        </div>
    </div>
</body>
</html>